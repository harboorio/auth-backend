/*
 * method is "get" by default
 * url is dirname by default
 * schema can be absent
 * handler can be absent, returns empty by default
 */

export function handler() {
    return { empty: true };
}
